{
  "scripts": {
    "build": "npx prisma generate && npx prisma db push && next build",
    "start": "next start",
    "dev": "next dev"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18",
    "@prisma/client": "^5.0.0" 
  }
}
